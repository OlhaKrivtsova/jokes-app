// import styles from './AddJoke.module.css'

interface AddJokeProps {
  children?: React.ReactNode;
}

const AddJoke = (props: AddJokeProps) => {
  return <div className='centered'>Add Joke</div>;
};

export default AddJoke;
